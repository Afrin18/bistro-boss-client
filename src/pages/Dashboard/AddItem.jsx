import { Helmet } from 'react-helmet-async';

const AddItem = () => {
    return (
        <div>
            <Helmet>
                <title>Bistro Boss | Add Item</title>
            </Helmet>
        </div>
    );
};

export default AddItem;